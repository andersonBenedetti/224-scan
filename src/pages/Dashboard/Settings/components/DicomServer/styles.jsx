import styled from 'styled-components';

export const Form = styled.form`
  max-width: 700px;
  display: flex;
  flex-direction: column;
  align-items: end;
  gap: 10px;

  textarea {
    height: 255px;
  }
`;
